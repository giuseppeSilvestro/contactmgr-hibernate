<hibernate-configuration>
    <session-factory>
        <!-- Database connection setting -->
        <property name="connection.driver_class">org.h2.Driver</property>
        <property name="connection.url">jdbc:h2:./data/contact.mgr</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.H2Dialect</property>

        <!-- Create the database schema on startup
         hdm2ddl stands for 'hibernate mapping to data definition language'-->
        <property name="hdm2ddl.auto">create</property>

        <!-- Names the annotated entity classes -->
        <mapping class="com.gsilvestro.contactmgr.model.Contact" />
    </session-factory>
</hibernate-configuration>